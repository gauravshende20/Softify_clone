server:
  port: 8090
spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/harmonia_payment?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root123}
harmonia:
  payments:
    stripe:
      secret-key: ${STRIPE_SECRET_KEY:}
      webhook-secret: ${STRIPE_WEBHOOK_SECRET:}
      premium-monthly-price-id: ${STRIPE_PRICE_ID_PREMIUM_MONTHLY:}
      public-url: ${APP_PUBLIC_URL:http://localhost:4200}
