server:
  port: 8088
spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/harmonia_analytics?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root123}
